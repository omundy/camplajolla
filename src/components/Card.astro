---
const { url, img, title, text } = Astro.props
import { Str } from '@supercharge/strings'
import * as GLOBALS from '../lib/globals.mjs'

// console.log(img)
let images = img.split(',')
// console.log(images)
const { src = `${GLOBALS.ASSETS_URL}/img/project-images/${images[0]}` } = Astro.props
---

<div class="card rounded-0" style="max-width: 18rem;">
  <a href={url} tabindex="0">
    <div class="row g-0">
      <div class="col-5">
        <!-- <div class="card-img rounded-start" style={'background-image: url(' + src + ');'}></div> -->
        <img src={src} alt="" class="img-fluid" style="width:101%" />
      </div>
      <div class="col-7">
        <div class="card-body m-0 p-0">
          <div class="card-title my-1 mx-2 p-0">{title}</div>
        </div>
      </div>
    </div>
  </a>
</div>

<style lang="scss">
  .card {
    --bs-card-border-width: 0 !important;
    border-left: 0.4rem solid var(--background);
    overflow: hidden;
    background-color: var(--background-50);
    height: 90px;
    transition-property: transform;
    transition-duration: 0.2s;
    filter: grayscale(1);
    
    a {
      color: var(--text-color);
      text-decoration: none;
    }

    &:hover,
    &:focus,
    &:focus-within,
    &:focus-within a:focus {
      box-shadow: none !important;
      background-color: var(--background-50);
    }

    &:hover,
    &:focus,
    &:focus-within {
      border-left: 0.43rem solid var(--orange);
      filter: saturate(1.1);
    }

    .card-img {
      width: 100%;
      height: 160px;
      background-repeat: none;
      background-size: cover;
      background-position: center center;
    }
    //   @media (min-width: 992px) {
    .card-img {
      width: 100%;
      height: 100px;
    }
    //   }

    .card-title {
      box-sizing: border-box;
      font-size: 0.9rem;
      padding: 0.5rem;
    }
  }
</style>
